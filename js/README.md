# Документація: Скрипт імпорту даних з Notion

## Зміст
1. [Огляд](#огляд)
2. [Архітектура](#архітектура)
3. [Конфігурація](#конфігурація)
4. [Процес роботи](#процес-роботи)
5. [Функції та модулі](#функції-та-модулі)
6. [Використання](#використання)
7. [Обробка помилок](#обробка-помилок)

---

## Огляд

Скрипт `notion_data_import.js` автоматизує процес імпорту та синхронізації даних з Notion бази даних до локальних JSON файлів. Він працює з трьома основними базами даних:

- **Аніме тайтли** - каталог аніме з деталями
- **Релізи** - інформація про переклади команд
- **Команди** - дані про фандаб команди

### Основні можливості

✅ Інкрементальне оновлення (тільки змінені записи)  
✅ Інтеграція з зовнішніми API (Hikka, Hikka-Forge, Mikai)  
✅ Автоматичний fallback між джерелами даних  
✅ Збагачення даних постерами з GitHub  
✅ Автоматичне оновлення статусів релізів  
✅ Кольоровий вивід в консоль для моніторингу  

---

## Архітектура

```
┌─────────────────────────────────────────────────────────┐
│                   Notion Databases                       │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐    │
│  │ Anime Titles │ │   Releases   │ │    Teams     │    │
│  └──────────────┘ └──────────────┘ └──────────────┘    │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              notion_data_import.js                       │
│  ┌──────────────────────────────────────────────────┐   │
│  │  1. Отримання сторінок з Notion                  │   │
│  │  2. Фільтрація змінених записів                  │   │
│  │  3. Обробка та трансформація даних               │   │
│  │  4. Збагачення з зовнішніх API                   │   │
│  │  5. Збереження до JSON файлів                    │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              External APIs                               │
│  ┌───────────┐ ┌──────────────┐ ┌──────────────┐       │
│  │   Hikka   │ │ Hikka-Forge  │ │    Mikai     │       │
│  └───────────┘ └──────────────┘ └──────────────┘       │
│  ┌──────────────────────────────────────────────┐       │
│  │  GitHub - UA Posters Repository              │       │
│  └──────────────────────────────────────────────┘       │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              Output JSON Files                           │
│  ┌──────────────────────────────────────────────────┐   │
│  │  AnimeTitlesDB.json                              │   │
│  │  AnimeReleasesDB.json                            │   │
│  │  TeamsDB.json                                    │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

---

## Конфігурація

### Константи та налаштування

```javascript
const DATABASES = {
  ANIME_TITLES_DB: "174d30fa35d081fb8baccf7e405d5cf9",
  ANIME_RELEASES_DB: "174d30fa35d081278dcdf4335e149330",
  TEAMS_DB: "174d30fa35d081c4968cc340c89e4667"
}

const UPDATE_MODES = {
  NONE: 'none',      // Не оновлювати
  MISSING: 'missing', // Тільки відсутні поля
  ALL: 'all'         // Оновити все
}

const HIKKA_FAILURE_THRESHOLD = 7 // Поріг помилок для перемикання на Forge
```

### Змінні середовища

Створіть файл `.env` у батьківській директорії:

```env
NOTION_TOKEN=secret_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

---

## Процес роботи

### 1. Ініціалізація

Скрипт запускається через функцію `runAllImports()`:

```javascript
await runAllImports({
  anime: { 
    onlyModified: true, 
    update: { hikka: 'missing', mikai: 'missing' } 
  },
  releases: { onlyModified: true },
  teams: { onlyModified: true }
})
```

### 2. Імпорт аніме тайтлів

**Крок 2.1: Завантаження сторінок з Notion**
- Отримує всі записи з бази "Аніме тайтли"
- Використовує пагінацію для великих баз даних
- Виводить прогрес у консоль

**Крок 2.2: Фільтрація змінених записів**
```javascript
filterModifiedPages(allPages, existingData)
```
Порівнює `last_edited_time` кожного запису з попереднім станом:
- Нові записи → додаються до обробки
- Змінені записи → додаються до обробки (якщо різниця > 1 секунди)
- Незмінені → пропускаються (якщо `onlyModified: true`)

**Крок 2.3: Обробка даних**
```javascript
processAnimeData(pages)
```
Для кожної сторінки:
1. Витягує дані з Notion властивостей
2. Завантажує попередні дані для збереження історичних полів
3. Збагачує постерами з GitHub репозиторію
4. Додає Mikai посилання за MAL ID

**Крок 2.4: Оновлення Hikka даних**

Система fallback працює наступним чином:

```
┌─────────────────────────────────────────────┐
│ Спроба отримати дані з Hikka API            │
└─────────────────┬───────────────────────────┘
                  │
          ┌───────▼───────┐
          │  Успішно?     │
          └───┬───────┬───┘
              │       │
           ТАК│       │НІ
              │       │
              │       └──────────┐
              │                  │
              │         ┌────────▼────────────────┐
              │         │ hikkaFailureCount++     │
              │         └────────┬────────────────┘
              │                  │
              │         ┌────────▼────────────────┐
              │         │ Якщо count >= 7?        │
              │         └───┬──────────────┬──────┘
              │             │              │
              │          ТАК│              │НІ
              │             │              │
              │    ┌────────▼─────┐        │
              │    │useForgeOnly  │        │
              │    │   = true     │        │
              │    └────────┬─────┘        │
              │             │              │
              │             │              │
              │             ▼              ▼
              │         ┌──────────────────────┐
              │         │ Спроба Hikka-Forge   │
              │         └──────────┬───────────┘
              │                    │
              │            ┌───────▼──────┐
              │            │  Успішно?    │
              │            └───┬──────┬───┘
              │                │      │
              │             ТАК│      │НІ
              │                │      │
              ▼                ▼      ▼
      ┌───────────────┐  ┌──────────────────┐
      │  Дані готові  │  │  Помилка логу    │
      └───────────────┘  └──────────────────┘
```

**Крок 2.5: Оновлення Mikai посилань**
- Завантажує дані з Mikai API
- Створює Map для швидкого пошуку за MAL ID
- Додає посилання типу `https://mikai.me/anime/{id}-{slug}`

**Крок 2.6: Злиття та збереження**
```javascript
mergeData(previousData, processedData)
```
- Об'єднує нові дані з існуючими (нові перезаписують старі)
- Зберігає результат у `AnimeTitlesDB.json`

### 3. Імпорт релізів

**Структура процесу:**

1. **Отримання сторінок** → аналогічно аніме
2. **Фільтрація змінених** → порівняння `last_edited_time`
3. **Обробка даних** → витягування властивостей Notion

**Особливості:**

**Відстеження оновлень епізодів:**
```javascript
episodesLastUpdate: previousRelease && previousRelease.episodes !== currentEpisodes
  ? new Date().toISOString()
  : previousRelease?.episodesLastUpdate || null
```
Якщо кількість епізодів змінилася → оновлюється timestamp

**Відстеження нових торрент-посилань:**
```javascript
hasNewTorrentLinks(currentLinks, previousLinks)
```
Порівнює масиви посилань і встановлює timestamp при додаванні нових

### 4. Імпорт команд

**Крок 4.1: Обробка даних команд**
```javascript
processTeamData(pages)
```
Витягує інформацію:
- Логотип та обкладинка
- Назва та синоніми
- Типи команди та активності
- Соціальні посилання
- Склад команди

**Крок 4.2: Формування зв'язків з релізами**
```javascript
buildTeamReleases(teamsData, releasesData)
```

Для кожного релізу:
1. Отримує список команд (основні + колаборанти)
2. Додає ID релізу до масиву `anime_releases` кожної команди
3. Видаляє дублікати

```
Release A (teams: [Team1, Team2], teamscolab: [Team3])
    │
    ├──▶ Team1.anime_releases.push({id: ReleaseA})
    ├──▶ Team2.anime_releases.push({id: ReleaseA})
    └──▶ Team3.anime_releases.push({id: ReleaseA})
```

### 5. Оновлення статусів закинутих релізів

```javascript
updateAbandonedReleases(releasesData, teamsData)
```

**Логіка:**

```
Для кожного релізу зі статусом "В процесі" або "Відкладено":
  │
  ├─▶ Отримати всі команди релізу (основні + колаборанти)
  │
  ├─▶ Перевірити статус кожної команди
  │
  └─▶ Якщо ВСІ команди неактивні:
      │
      └─▶ Змінити статус релізу на "Закинуто"
```

**Неактивні статуси команд:**
- Невідомо
- Неактивна
- Припинено
- Розформована

---

## Функції та модулі

### Утиліти

#### `colorLog(message, color, mode)`
Виводить кольоровий текст у консоль.

**Параметри:**
- `message` - текст для виводу
- `color` - колір (`'red'`, `'green'`, `'yellow'`, `'blue'`, `'reset'`)
- `mode` - режим виводу:
  - `NEWLINE` - новий рядок (за замовчуванням)
  - `SAMELINE` - перезапис поточного рядка
  - `PROGRESS` - прогрес-бар стиль

**Приклад:**
```javascript
colorLog('Завантажено 50 записів', 'green', OUTPUT_MODES.PROGRESS)
```

#### `loadPreviousData(fileName)`
Завантажує попередні дані з JSON файлу.

**Шлях:** `../../CPRcatalog/json/{fileName}`

**Повертає:** 
- Array з даними (якщо файл існує)
- Порожній масив (якщо файл не знайдено)

#### `saveData(fileName, data)`
Зберігає дані у JSON файл з форматуванням.

**Форматування:** `JSON.stringify(data, null, 2)` (2 пробіли відступ)

#### `loadExternalData(url, name)`
Завантажує дані з зовнішнього API через axios.

**Обробка помилок:** Логує помилку та повертає порожній масив

### Робота з Notion

#### `getAllPages(databaseId, dbTitle)`
Отримує всі сторінки з Notion бази даних з пагінацією.

**Процес:**
```javascript
while (hasMore) {
  const response = await Notion.databases.query({
    database_id: databaseId,
    start_cursor: nextCursor || undefined
  })
  pages = pages.concat(response.results)
  hasMore = response.has_more
  nextCursor = response.next_cursor
}
```

**Вивід прогресу:** Оновлюється після кожної порції сторінок

#### `filterModifiedPages(allPages, existingData)`
Відфільтровує тільки нові та змінені сторінки.

**Алгоритм:**
1. Створює Map з існуючих даних: `{id → last_edited_time}`
2. Для кожної сторінки:
   - Нова (ID немає в Map) → додати до `modifiedPages`
   - Існуюча з новішою датою → додати до `modifiedPages`
   - Незмінена → пропустити

**Толерантність часу:** 1 секунда (для уникнення проблем точності)

### API інтеграції

#### `extractSlugFromUrl(hikkaUrl)`
Витягує slug з Hikka URL.

**Приклад:**
```javascript
'https://hikka.io/anime/frieren' → 'frieren'
```

#### `fetchHikkaData(slug)`
Отримує дані з основного Hikka API.

**Endpoint:** `https://api.hikka.io/anime/{slug}`

**Повертає об'єкт:**
```javascript
{
  hikka_poster: anime.image,
  hikkaSynonyms: anime.synonyms,
  status: anime.status,
  season: anime.season,
  duration: anime.duration,
  scoreMAL: anime.score,
  scoredbyMAL: anime.scored_by,
  scoreHikka: anime.native_score,
  scoredbyHikka: anime.native_scored_by,
  source: anime.source,
  mal_id: anime.mal_id
}
```

#### `fetchHikkaForgeData(slug)`
Fallback - отримує дані з Hikka-Forge API.

**Endpoint:** `https://hikka-forge.lorgon.dev/anime/{slug}`

**Відмінності від Hikka:**
- Менше полів (немає synonyms, status, season, тощо)
- Використовується тільки як резервне джерело
- Більша надійність

#### `fetchHikkaDataWithFallback(urls)`
Основна функція отримання даних з автоматичним fallback.

**Алгоритм:**
```javascript
for (url of urls) {
  if (!useForgeOnly) {
    data = await fetchHikkaData(slug)
    if (!data) {
      hikkaFailureCount++
      if (hikkaFailureCount >= 7) {
        useForgeOnly = true
      }
    }
  }
  
  if (!data) {
    data = await fetchHikkaForgeData(slug)
  }
  
  if (data) results.push(data)
}
```

**Затримка:** 100ms між запитами (rate limiting)

#### `updateHikkaData(animeData, mode)`
Оновлює Hikka дані для аніме за заданим режимом.

**Режими:**
- `NONE` - пропустити оновлення
- `MISSING` - оновити тільки записи з відсутніми полями
- `ALL` - оновити всі записи

**Перевірка необхідності оновлення:**
```javascript
function needsHikkaUpdate(anime, mode, previousData) {
  if (!anime.hikka_url) return false
  if (mode === 'ALL') return true
  if (mode === 'MISSING') {
    const existing = previousData.find(e => e.hikka_url === anime.hikka_url)
    return !existing || getMissingHikkaFields(existing).length > 0
  }
  return false
}
```

#### `updateMikaiLinks(animeData, mode)`
Додає посилання на Mikai за MAL ID.

**Процес:**
1. Завантажує повний список з Mikai API
2. Створює Map: `{malId → {id, slug, url}}`
3. Для кожного аніме з MAL ID додає посилання

**Формат URL:** `https://mikai.me/anime/{id}-{slug}`

### Обробка даних

#### `buildAnimeData(page, previousAnime, posterList, mikaiUrl)`
Конструює об'єкт аніме з Notion сторінки.

**Властивості Notion → JSON:**
```javascript
{
  'Назва тайтлу' → title,
  'Ромаджі' → romaji,
  'Синоніми' → synonyms,
  'Тип медіа' → type,
  'Формат' → format,
  'Рік виходу' → year,
  'Жанри' → genre,
  'Кількість серій' → episodes,
  // ... та інші
}
```

**Особливості:**
- Зберігає попередні Hikka дані (щоб не втратити при реімпорті)
- Додає poster URL з GitHub репозиторію
- Форматує синоніми як масив (розділяє по `\n`)

#### `buildReleaseData(page, previousRelease)`
Конструює об'єкт релізу.

**Спеціальна логіка:**

**Торрент посилання:**
```javascript
currentTorrentLinks = page.properties['Торент посилання'].rich_text
  .filter(link => link)
  .map(link => ({
    text: link.plain_text.trim(),
    href: link.href
  }))
```

**Timestamps оновлень:**
- `episodesLastUpdate` - коли змінилася кількість епізодів
- `torrentLinksLastAdded` - коли додано нові торрент посилання

#### `buildTeamData(page)`
Конструює об'єкт команди.

**Поля:**
```javascript
{
  id, cover, logo, name, altname,
  founded, type_team, type_activity,
  status, members, anime_releases,
  site, anitube, youtube, insta,
  tiktok, tg, tg_video,
  created_time, last_edited
}
```

#### `buildTeamReleases(teamsData, releasesData)`
Формує двосторонні зв'язки між командами та релізами.

**Приклад:**
```
Вхід:
  Team1: { anime_releases: [] }
  Release1: { teams: [Team1.id], teamscolab: [Team2.id] }

Вихід:
  Team1: { anime_releases: [{ id: Release1.id }] }
  Team2: { anime_releases: [{ id: Release1.id }] }
```

### Головні функції

#### `getAnimeTitlesJson(options)`
Повний процес імпорту аніме тайтлів.

**Опції:**
```javascript
{
  onlyModified: true,  // Тільки змінені записи?
  update: {
    hikka: 'missing',  // 'none' | 'missing' | 'all'
    mikai: 'missing'   // 'none' | 'missing' | 'all'
  }
}
```

**Повертає:** Масив всіх аніме тайтлів (старі + нові)

#### `getReleasesJson(options)`
Повний процес імпорту релізів.

**Опції:**
```javascript
{
  onlyModified: true
}
```

#### `getTeamsJson(releasesData, options)`
Повний процес імпорту команд.

**Параметри:**
- `releasesData` - потрібні для формування зв'язків
- `options.onlyModified` - фільтр змінених

#### `runAllImports(options)`
Оркеструє весь процес імпорту.

**Порядок виконання:**
1. Аніме тайтли (незалежний)
2. Релізи (незалежний)
3. Команди (залежить від релізів)
4. Оновлення закинутих релізів (залежить від команд і релізів)

---

## Використання

### Базове використання

```javascript
// Запуск з налаштуваннями за замовчуванням
await runAllImports()
```

### Повне оновлення

```javascript
await runAllImports({
  anime: { 
    onlyModified: false,  // Обробити всі записи
    update: { 
      hikka: 'all',       // Оновити всі Hikka дані
      mikai: 'all'        // Оновити всі Mikai посилання
    } 
  },
  releases: { onlyModified: false },
  teams: { onlyModified: false }
})
```

### Вибіркове оновлення

```javascript
// Оновити тільки аніме, не чіпати релізи та команди
await getAnimeTitlesJson({
  onlyModified: true,
  update: { hikka: 'missing', mikai: 'none' }
})
```

### Запуск через CLI

```bash
# Базовий запуск
node notion_data_import.js

# З перевіркою помилок
node notion_data_import.js || echo "Помилка імпорту!"
```

### Автоматизація через cron

```bash
# Щодня о 3:00 ранку
0 3 * * * cd /path/to/project && node notion_data_import.js >> logs/import.log 2>&1
```

---

## Обробка помилок

### Типи помилок

**1. Помилки мережі**
```javascript
try {
  const response = await axios.get(url)
} catch (error) {
  colorLog(`Не вдалося завантажити: ${error.message}`, 'yellow')
  return []
}
```
**Стратегія:** Логування та повернення порожнього масиву

**2. Помилки Notion API**
```javascript
try {
  return await Notion.pages.retrieve({ page_id: pageId })
} catch (error) {
  console.error('Помилка при отриманні даних:', error)
  throw error
}
```
**Стратегія:** Логування та пробрасування помилки (критична)

**3. Помилки збереження**
```javascript
try {
  await fs.writeFile(path, data)
  colorLog('Успішно збережено', 'green')
} catch (error) {
  colorLog(`Помилка збереження: ${error.message}`, 'red')
  throw error
}
```
**Стратегія:** Логування та пробрасування (критична)

### Retry логіка

Для Hikka API реалізовано fallback замість retry:

```
Hikka API помилка
  ↓
Лічильник помилок++
  ↓
Якщо >= 7 помилок підряд
  ↓
Перемикання на Hikka-Forge
  ↓
Всі наступні запити йдуть на Forge
```

### Логування

**Рівні важливості (за кольорами):**
- 🔴 `red` - критичні помилки
- 🟡 `yellow` - попередження, пропуски
- 🟢 `green` - успішні операції
- 🔵 `blue` - інформаційні повідомлення
- ⚪ `reset` - нейтральна інформація

**Приклад виводу:**
```
🔵 Початок імпорту сторінок з бази даних Аніме тайтли
⚪ Отримано 100 сторінок...
⚪ Отримано 200 сторінок...
⚪ Завершено отримання сторінок. Всього: 237
🟢 Нових сторінок: 5
🟡 Змінених існуючих: 12
🔵 Оновлення Hikka даних (режим: missing)...
🟢 Обробка: 15/17. frieren [Hikka]
🟡 Hikka помилка (1/7): bocchi-the-rock
🔵 Обробка: 16/17. bocchi-the-rock [Forge]
🟢 Оновлено Hikka даних: 17
🟢 Успішно збережено дані у файл AnimeTitlesDB.json: 237 записів
```

---

## Структура даних

### AnimeTitlesDB.json

```json
[
  {
    "id": "174d30fa-35d0-81e1-92db-f94375dde776",
    "hikka_url": "https://hikka.io/anime/frieren",
    "hikka_poster": "https://cdn.hikka.io/...",
    "cover": "https://...",
    "poster": "https://raw.githubusercontent.com/...",
    "posters": [
      {
        "url": "Frieren/poster_01.jpg",
        "author": "username"
      }
    ],
    "title": "Фрірен: Поза кінцем шляху",
    "romaji": "Sousou no Frieren",
    "synonyms": ["Frieren at the Funeral", "葬送のフリーレン"],
    "hikkaSynonyms": [...],
    "type": "ТБ-серіал",
    "format": "TV",
    "format_cpr": "ТБ-серіал",
    "year": "2023",
    "genre": "Фентезі",
    "status": "finished_airing",
    "season": "fall_2023",
    "duration": 1421,
    "scoreMAL": 9.31,
    "scoredbyMAL": 234567,
    "scoreHikka": 9.45,
    "scoredbyHikka": 12345,
    "anitube": "https://anitube.in.ua/...",
    "uaserial": null,
    "uakino": null,
    "mikai": "https://mikai.me/anime/52991-frieren-poza-kincyem-shlyahu",
    "tg_channel": "https://t.me/...",
    "episodes": "28",
    "releases": [
      { "id": "release-id-1" },
      { "id": "release-id-2" }
    ],
    "relations": [],
    "franchise": [],
    "source": "manga",
    "mal_id": 52991,
    "created_time": "2024-01-15T10:30:00.000Z",
    "last_edited": "2024-01-17T14:22:00.000Z"
  }
]
```

### AnimeReleasesDB.json

```json
[
  {
    "id": "release-id-1",
    "title": "Фрірен [ukrainian_team]",
    "animeIds": ["174d30fa-35d0-81e1-92db-f94375dde776"],
    "teams": ["team-id-1"],
    "teamscolab": ["team-id-2"],
    "dubinfo": ["Багатоголосий"],
    "subinfo": ["Українські субтитри"],
    "status": "Завершено",
  // ... дописати
  }
]
```